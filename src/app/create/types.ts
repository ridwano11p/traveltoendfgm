export interface CreateOptionProps {
    title: string;
    description: string;
    link: string;
    disabled?: boolean;
  }
  
  export interface ContentExistence {
    [key: string]: boolean;
  }
  